import { SxProps } from "@mui/material";

export const cardContainer: SxProps = {
  display: "flex",
  flexDirection: "column",
  flexWrap: "wrap",
  padding: "12px",
  gap: "12px",
  boxShadow: "none",
  overflowY: "auto",
}